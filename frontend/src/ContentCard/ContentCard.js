import './ContentCard_Style.css';


const ContentCard = ({content}) => {
    const {type, title, description, preview_img, mediaURL} = content;

    return(
        <div>

        </div>
    );
};

export default ContentCard;


